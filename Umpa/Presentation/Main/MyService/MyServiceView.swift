// Created for Umpa in 2025

import SwiftUI

struct MyServiceView: View {
    var body: some View {
        VStack {
            Text("내 서비스 목록")
                .font(.headline)
            // 여기에 서비스 목록 표시 로직 추가
        }
    }
}

#Preview {
    MyServiceView()
}
